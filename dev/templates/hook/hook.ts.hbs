import { useState } from 'react'

type Props = object

type ReturnValue = unknown

const {{camelCase name}} = ({}: Props): ReturnValue => {
  const [value, setValue] = useState('')

  return {
    value,
    setValue
  }
}

export default {{camelCase name}}
